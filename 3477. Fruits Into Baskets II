class Solution {
public:
    int numOfUnplacedFruits(vector<int>& fruits, vector<int>& baskets) {
        int n = fruits.size(), book = 0;

        for(int i=0;i<n;i++)
        {
            for(int j=0;j<baskets.size();j++)
            {
                if(fruits[i] <= baskets[j])
                {
                    book++;
                    baskets[j]=-1;
                    break;
                }
            }
        }
        return n-book;
    }
};
